import XCTest
@testable import swift_validation

final class swift_validationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_validation().text, "Hello, World!")
    }
}
